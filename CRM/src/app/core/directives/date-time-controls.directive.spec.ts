import { DateTimeControlsDirective } from './date-time-controls.directive';

describe('DateTimeControlsDirective', () => {
  it('should create an instance', () => {
    const directive = new DateTimeControlsDirective();
    expect(directive).toBeTruthy();
  });
});
